// Every single time you see this in JavaScript, ask yourself: what is the call-site? Then apply one of these 4 rules in order.

// 🕵️ Think like a detective
// Finding what this refers to is detective work. Your evidence is the call-site — the exact line where the function is called (not defined). Look at how the function is being invoked, then match it to one of the four rules below.
